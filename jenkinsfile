pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        echo 'Executing build stage'
        sh 'echo "Hello"'
      }
    }

    stage('deploy') {
      steps {
        echo 'Executing deploy stage'
      }
    }

  }
}
